name: DOGE-TEST

on:
  [workflow_dispatch, push]
jobs:
  doge-test:
    name: SIMPLE test
    runs-on: ubuntu-latest
    steps:
      - uses: kuritka/doge-action@v0.0.7
        name: DOGE-ACTION
        with:
          cluster-name: "blaaah"
